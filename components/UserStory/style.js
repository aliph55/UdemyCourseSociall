import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  storyContainer: {
    marginRight: 20,
  },
  name: {
    textAlign: "center",
    fontWeight: "500",
    fontSize: 14,
    lineHeight: 17,
    marginTop: 8,
  },
});

export default style;
